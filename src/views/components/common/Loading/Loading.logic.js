// const broadcast = require('@/broadcast/broadcast');
// var m = require("mithril");

const logic = {
    oninit(vnode) {
    },
    oncreate(vnode) {
    },
    onupdate(vnode) {
    },
    onremove(vnode) {
    }
};

module.exports = logic;
